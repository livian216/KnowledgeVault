# uv 新手入门使用教程

> 面向对象：使用过 `conda`、`venv`、`pip`，但从未使用过 `uv` 的 Python 开发者  
> 主要环境：Windows 11 + PowerShell，同时兼顾 Linux/macOS  
> 文档更新日期：2026-07-27

---

## 1. uv 是什么

`uv` 是 Astral 开发的 Python 包管理与项目管理工具，使用 Rust 编写。它可以覆盖许多原本需要多个工具共同完成的工作，例如：

- 安装和管理 Python 版本；
- 创建虚拟环境；
- 安装、升级和删除 Python 包；
- 管理 `pyproject.toml`；
- 解析并锁定依赖版本；
- 根据锁文件复现项目环境；
- 运行 Python 脚本和项目命令；
- 临时运行 Ruff、Black 等命令行工具。

可以把它理解为：

```text
uv ≈ Python 版本管理 + venv + pip + pip-tools + 部分 poetry 功能
```

不过，`uv` 不是 `conda` 的完全替代品。

`conda` 不仅能管理 Python 包，还能管理 CUDA、MKL、FFmpeg、GDAL 等非 Python 二进制依赖；而 `uv` 的主要职责仍然是管理 Python、Python 虚拟环境和 Python 包。

因此：

- 常规 Python 项目、Web 项目、自动化脚本、纯 Python 算法项目：适合使用 `uv`；
- 强依赖复杂 CUDA、系统库或非 Python 软件包的科研环境：可以继续使用 `conda`，也可以采用“conda 管底层环境，uv 管 Python 依赖”的组合方式；
- 对于 PyTorch、TensorFlow 等项目，应先确认目标 CUDA 版本、显卡驱动和安装源，再决定是否完全迁移到 `uv`。

---

## 2. 从 conda、venv 迁移到 uv，需要建立什么概念

### 2.1 传统工作方式

使用 `venv + pip` 时，常见流程是：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install requests
pip freeze > requirements.txt
python main.py
```

使用 `conda` 时，常见流程是：

```powershell
conda create -n demo python=3.12
conda activate demo
pip install requests
python main.py
```

### 2.2 uv 推荐的项目工作方式

使用 `uv` 后，推荐流程是：

```powershell
uv init
uv add requests
uv run python main.py
```

这几条命令背后，`uv` 会自动完成以下工作：

1. 读取 `pyproject.toml`；
2. 解析项目依赖；
3. 创建或更新 `uv.lock`；
4. 创建项目虚拟环境 `.venv`；
5. 安装锁定版本的依赖；
6. 在该项目环境中运行命令。

### 2.3 uv 的两种使用模式

新手最容易混淆的是，`uv` 同时提供两套用法。

#### 模式 A：项目模式，推荐用于新项目

核心命令：

```text
uv init
uv add
uv remove
uv sync
uv lock
uv run
```

依赖记录在：

```text
pyproject.toml
uv.lock
```

这是最推荐的方式。

#### 模式 B：pip 兼容模式，适合旧项目或临时环境

核心命令：

```text
uv venv
uv pip install
uv pip uninstall
uv pip list
uv pip freeze
```

它的逻辑接近：

```text
python -m venv
pip install
pip uninstall
pip list
pip freeze
```

如果项目仍然以 `requirements.txt` 为中心，可以先使用这一模式，之后再迁移到项目模式。

> 初学阶段建议：新项目使用项目模式；接手旧项目时，再根据项目结构决定是否使用 pip 兼容模式。

---

## 3. 在 Windows 11 上安装 uv

### 3.1 使用官方 PowerShell 安装脚本

打开 PowerShell，执行：

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

安装完成后，关闭当前 PowerShell 窗口，再重新打开一个 PowerShell。

验证安装：

```powershell
uv --version
```

查看命令位置：

```powershell
Get-Command uv
```

### 3.2 使用 WinGet 安装

也可以使用：

```powershell
winget install --id=astral-sh.uv -e
```

安装后重新打开 PowerShell，再执行：

```powershell
uv --version
```

### 3.3 安装后仍提示找不到 uv

先检查常见安装位置：

```powershell
Test-Path "$HOME\.local\bin\uv.exe"
```

如果返回：

```text
True
```

说明 `uv.exe` 已经存在，但当前终端的 `PATH` 尚未刷新。

可以先临时加入当前 PowerShell：

```powershell
$env:Path += ";$HOME\.local\bin"
uv --version
```

然后关闭并重新打开终端。

### 3.4 更新 uv

使用官方独立安装器安装时，一般可以执行：

```powershell
uv self update
```

再次查看版本：

```powershell
uv --version
```

---

## 4. 第一个 uv 项目：完整入门示例

假设要在下面的目录创建项目：

```text
path\to\your\project
```

### 4.1 进入项目目录

```powershell
cd path\to\your\project
```

查看当前目录内容：

```powershell
Get-ChildItem
```

### 4.2 初始化项目

如果当前目录还不是 uv 项目，执行：

```powershell
uv init
```

`uv init` 通常会创建或补充以下文件：

```text
my_project/
├─ .gitignore
├─ .python-version
├─ README.md
├─ main.py
└─ pyproject.toml
```

如果是从零创建一个新目录，也可以直接执行：

```powershell
cd path\to\project
uv init my_project
cd path\to\project
```

### 4.3 查看 pyproject.toml

初始化后，`pyproject.toml` 可能类似：

```toml
[project]
name = "my_project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
```

其中：

- `name`：项目名称；
- `version`：项目版本；
- `requires-python`：允许使用的 Python 版本范围；
- `dependencies`：项目运行所需的直接依赖。

### 4.4 添加第一个依赖

例如添加 `requests`：

```powershell
uv add requests
```

此时 uv 会：

- 将 `requests` 写入 `pyproject.toml`；
- 解析它的间接依赖；
- 生成或更新 `uv.lock`；
- 创建 `.venv`；
- 将依赖安装到 `.venv`。

查看依赖树：

```powershell
uv tree
```

### 4.5 编写代码

修改 `main.py`：

```python
import requests

response = requests.get(
    "https://httpbin.org/get",
    timeout=10,
)

response.raise_for_status()
print(response.json())
```

### 4.6 运行代码

推荐方式：

```powershell
uv run python main.py
```

通常也可以简写为：

```powershell
uv run main.py
```

`uv run` 会先确认：

- `pyproject.toml` 与 `uv.lock` 是否一致；
- `.venv` 与 `uv.lock` 是否一致；
- 缺失依赖是否需要安装。

之后再在项目虚拟环境中运行命令。

---

## 5. 是否还需要手动激活虚拟环境

### 5.1 推荐：不激活，直接使用 uv run

```powershell
uv run python main.py
uv run pytest
uv run ruff check .
```

优点：

- 不容易误用系统 Python；
- 不需要记住环境是否已经激活；
- 每次运行前都会检查项目依赖；
- 更适合脚本、CI 和团队协作。

### 5.2 仍然可以像以前一样激活环境

先同步环境：

```powershell
uv sync
```

然后在 PowerShell 中激活：

```powershell
.\.venv\Scripts\Activate.ps1
```

激活后可以直接执行：

```powershell
python main.py
pip list
```

退出虚拟环境：

```powershell
deactivate
```

### 5.3 PowerShell 禁止执行 Activate.ps1

如果出现“系统禁止运行脚本”之类的错误，可以为当前用户调整策略：

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

执行后重新打开 PowerShell，再激活：

```powershell
.\.venv\Scripts\Activate.ps1
```

如果不希望修改 PowerShell 执行策略，直接使用 `uv run` 即可，不需要激活环境。

---

## 6. 理解 uv 项目中的四个关键对象

### 6.1 pyproject.toml

`pyproject.toml` 是项目声明文件，主要记录：

- 项目名称和版本；
- 支持的 Python 版本；
- 直接依赖；
- 开发依赖；
- 可选依赖；
- 构建配置；
- Ruff、Pytest 等工具配置。

它更接近“项目需要什么”。

### 6.2 uv.lock

`uv.lock` 是锁文件，记录解析后的精确依赖关系和版本信息。

它更接近“项目实际使用什么版本”。

建议：

```text
提交 uv.lock 到 Git
```

不要手动编辑 `uv.lock`。

### 6.3 .venv

`.venv` 是项目的实际虚拟环境，包含：

- Python 解释器入口；
- 已安装的包；
- 项目运行所需的环境文件。

建议：

```text
不要提交 .venv 到 Git
```

如果删除 `.venv`，可以通过下面的命令重新创建：

```powershell
uv sync
```

### 6.4 .python-version

`.python-version` 用于记录该项目期望使用的 Python 版本，例如：

```text
3.12
```

可以通过命令创建或修改：

```powershell
uv python pin 3.12
```

---

## 7. 使用 uv 管理 Python 版本

### 7.1 查看可用和已安装的 Python

```powershell
uv python list
```

### 7.2 安装指定 Python 版本

```powershell
uv python install 3.12
```

同时安装多个版本：

```powershell
uv python install 3.11 3.12
```

### 7.3 为项目固定 Python 版本

```powershell
uv python pin 3.12
```

该命令会创建或更新：

```text
.python-version
```

### 7.4 使用指定 Python 创建虚拟环境

```powershell
uv venv --python 3.12
```

也可以指定完整解释器路径：

```powershell
uv venv --python "C:\Python312\python.exe"
```

### 7.5 uv 会不会使用已有 Python

会。

如果 Windows 中已经安装了符合要求的 Python，uv 通常会自动发现并使用它。如果找不到满足项目要求的版本，uv 也可以按需下载受管理的 Python。

查看当前实际使用的解释器：

```powershell
uv run python -c "import sys; print(sys.executable)"
```

查看版本：

```powershell
uv run python --version
```

---

## 8. 管理项目依赖

### 8.1 添加普通依赖

```powershell
uv add requests
```

添加多个依赖：

```powershell
uv add numpy pandas matplotlib
```

指定版本范围：

```powershell
uv add "numpy>=2.0"
```

指定精确版本：

```powershell
uv add "numpy==2.2.6"
```

> PowerShell 中含有 `>、<、=` 等版本符号时，建议使用引号包围依赖表达式。

### 8.2 添加开发依赖

测试、格式化、静态检查等工具通常属于开发依赖：

```powershell
uv add --dev pytest ruff
```

`--dev` 是开发依赖组的快捷写法。

常见开发依赖包括：

```text
pytest
ruff
mypy
ipykernel
pre-commit
```

### 8.3 添加到自定义依赖组

例如单独建立文档依赖组：

```powershell
uv add --group docs mkdocs mkdocs-material
```

同步指定组：

```powershell
uv sync --group docs
```

### 8.4 删除依赖

```powershell
uv remove requests
```

删除开发依赖：

```powershell
uv remove --dev pytest
```

### 8.5 查看依赖树

```powershell
uv tree
```

### 8.6 更新某个依赖

先确认 `pyproject.toml` 中允许的版本范围，再执行：

```powershell
uv lock --upgrade-package requests
uv sync
```

也可以在重新声明版本约束时更新：

```powershell
uv add "requests>=2.32" --upgrade-package requests
```

### 8.7 更新全部允许升级的依赖

```powershell
uv lock --upgrade
uv sync
```

注意：

- 更新范围仍然受到 `pyproject.toml` 版本约束限制；
- 科研和生产项目不要在没有测试的情况下随意升级全部依赖；
- 更新后应运行测试并检查 `uv.lock` 的变化。

---

## 9. uv lock、uv sync 和 uv run 的区别

### 9.1 uv lock

```powershell
uv lock
```

作用：

- 读取 `pyproject.toml`；
- 解析完整依赖关系；
- 创建或更新 `uv.lock`。

它主要更新锁文件，不等同于手动运行程序。

### 9.2 uv sync

```powershell
uv sync
```

作用：

- 检查或更新锁文件；
- 创建 `.venv`；
- 按照 `uv.lock` 同步环境。

收到别人提供的 uv 项目后，通常首先执行：

```powershell
uv sync
```

### 9.3 uv run

```powershell
uv run python main.py
```

作用：

- 在项目环境中运行命令；
- 运行前自动检查锁文件和虚拟环境；
- 必要时自动锁定并同步。

### 9.4 简单记忆

```text
uv lock：确定应该装哪些版本
uv sync：把这些版本装进 .venv
uv run：在这个环境中执行命令
```

很多日常场景直接使用：

```powershell
uv run ...
```

即可。

---

## 10. 接手别人提供的 uv 项目

假设从 Git 克隆了一个包含以下文件的项目：

```text
pyproject.toml
uv.lock
.python-version
```

进入项目目录：

```powershell
cd path\to\project
```

同步环境：

```powershell
uv sync
```

运行项目：

```powershell
uv run python main.py
```

运行测试：

```powershell
uv run pytest
```

一般不需要：

- 手工创建 `.venv`；
- 手工逐个安装依赖；
- 从其他电脑复制 `.venv`；
- 手工执行 `pip freeze`。

---

## 11. 在已有项目中使用 uv

### 11.1 已经有 pyproject.toml

先进入项目目录：

```powershell
cd path\to\project
```

直接尝试：

```powershell
uv sync
```

如果项目尚无锁文件，uv 会创建 `uv.lock`。

然后运行：

```powershell
uv run python main.py
```

### 11.2 只有 requirements.txt：保守迁移方式

如果暂时不想改项目结构，可以先创建虚拟环境：

```powershell
uv venv
```

安装依赖：

```powershell
uv pip install -r requirements.txt
```

运行：

```powershell
uv run --no-project python main.py
```

或者激活环境后运行：

```powershell
.\.venv\Scripts\Activate.ps1
python main.py
```

这种方式仍以 `requirements.txt` 为中心，适合低风险试用 uv。

### 11.3 只有 requirements.txt：迁移到项目模式

先备份或提交当前 Git 状态，然后执行：

```powershell
uv init
uv add -r requirements.txt
```

之后检查：

```text
pyproject.toml
uv.lock
```

再执行：

```powershell
uv sync
uv run python main.py
```

如果原来的 `requirements.txt` 包含大量由 `pip freeze` 生成的间接依赖，最好先区分：

- 项目直接使用的依赖；
- 依赖自动带入的间接依赖。

否则会把大量间接包误写成项目的直接依赖。

### 11.4 保留旧项目当前锁定版本

如果项目同时有：

```text
requirements.in
requirements.txt
```

可以使用现有锁定版本作为约束：

```powershell
uv add -r requirements.in -c requirements.txt
```

---

## 12. uv 的 pip 兼容模式

### 12.1 创建虚拟环境

```powershell
uv venv
```

指定 Python：

```powershell
uv venv --python 3.12
```

指定环境目录：

```powershell
uv venv env
```

### 12.2 安装包

```powershell
uv pip install requests
```

安装多个包：

```powershell
uv pip install numpy pandas matplotlib
```

从文件安装：

```powershell
uv pip install -r requirements.txt
```

### 12.3 查看包

```powershell
uv pip list
```

查看冻结版本：

```powershell
uv pip freeze
```

### 12.4 删除包

```powershell
uv pip uninstall requests
```

### 12.5 与普通 pip 的重要差异

`uv pip` 默认倾向于使用虚拟环境，而不是直接修改系统 Python。

如果当前目录没有虚拟环境，先执行：

```powershell
uv venv
```

然后再执行：

```powershell
uv pip install requests
```

### 12.6 不要混淆 uv add 和 uv pip install

```powershell
uv add requests
```

表示：

- 把 `requests` 声明为项目依赖；
- 修改 `pyproject.toml`；
- 更新 `uv.lock`；
- 同步项目环境。

而：

```powershell
uv pip install requests
```

主要表示：

- 把包安装进当前虚拟环境；
- 不一定将其正式记录为项目依赖；
- 更接近传统 `pip install`。

对于正式 uv 项目，应优先使用：

```powershell
uv add
```

---

## 13. 使用 uv 运行临时命令行工具

uv 提供 `uvx`，适合临时运行 Python 命令行工具。

例如运行 Ruff：

```powershell
uvx ruff check .
```

运行 Black：

```powershell
uvx black .
```

`uvx` 等价于：

```powershell
uv tool run
```

它会为工具准备隔离环境，不必把工具永久加入当前项目依赖。

如果希望长期安装某个全局工具：

```powershell
uv tool install ruff
```

查看已安装工具：

```powershell
uv tool list
```

卸载工具：

```powershell
uv tool uninstall ruff
```

使用原则：

- 项目必须固定使用的工具：`uv add --dev ruff`；
- 偶尔临时使用：`uvx ruff ...`；
- 希望在电脑上长期全局使用：`uv tool install ruff`。

---

## 14. uv 与 Jupyter Notebook

### 14.1 在项目中添加 ipykernel

```powershell
uv add --dev ipykernel
```

### 14.2 临时启动 JupyterLab

```powershell
uv run --with jupyter jupyter lab
```

### 14.3 在 VS Code 中选择解释器

在 VS Code 中执行：

```text
Python: Select Interpreter
```

选择项目中的：

```text
项目目录\.venv\Scripts\python.exe
```

也可以在 PowerShell 中查看完整路径：

```powershell
uv run python -c "import sys; print(sys.executable)"
```

### 14.4 Notebook 中不要随意使用 !pip install

Notebook 内直接执行：

```python
!pip install package_name
```

可能会安装到错误环境。

更稳妥的做法是返回项目终端执行：

```powershell
uv add package_name
```

然后重启 Notebook 内核。

---

## 15. 导出 requirements.txt

uv 项目通常以：

```text
pyproject.toml + uv.lock
```

作为主要依赖描述和锁定方式。

如果某些部署平台只接受 `requirements.txt`，可以导出：

```powershell
uv export --format requirements.txt --output-file requirements.txt
```

然后其他环境可以使用：

```powershell
uv pip install -r requirements.txt
```

或者：

```powershell
pip install -r requirements.txt
```

一般不建议同时长期维护两套独立的锁文件：

```text
uv.lock
requirements.txt
```

否则容易出现版本不一致。更合理的方式是以 `uv.lock` 为准，在需要时自动导出 `requirements.txt`。

---

## 16. Git 版本管理建议

### 16.1 建议提交

```text
pyproject.toml
uv.lock
.python-version
README.md
源代码
测试代码
```

### 16.2 不建议提交

```text
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.pyc
```

`.gitignore` 中至少应包含：

```gitignore
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
```

### 16.3 为什么要提交 uv.lock

提交 `uv.lock` 后：

- 不同开发者能获得一致的依赖版本；
- CI 环境能复现开发环境；
- 可以审查依赖升级带来的变化；
- 部署过程更加稳定。

---

## 17. 常见问题与排查方法

### 17.1 uv 命令无法识别

报错：

```text
uv : 无法将“uv”项识别为 cmdlet、函数、脚本文件或可运行程序的名称
```

排查：

```powershell
Get-Command uv
where.exe uv
Test-Path "$HOME\.local\bin\uv.exe"
```

处理：

1. 确认 uv 已安装；
2. 关闭并重新打开 PowerShell；
3. 检查安装目录是否已加入 `PATH`；
4. 必要时重新安装 uv。

### 17.2 不知道当前用了哪个 Python

```powershell
uv run python -c "import sys; print(sys.executable)"
uv run python --version
```

激活环境后也可以：

```powershell
python -c "import sys; print(sys.executable)"
```

### 17.3 删除 .venv 后项目还能恢复吗

可以。

```powershell
Remove-Item -Recurse -Force .venv
uv sync
```

`uv sync` 会根据项目文件重新创建环境。

### 17.4 pyproject.toml 改了但环境没变化

执行：

```powershell
uv sync
```

如果是运行命令，也可以直接：

```powershell
uv run python main.py
```

`uv run` 通常会自动检查并同步。

### 17.5 包安装了但 import 失败

先确认解释器：

```powershell
uv run python -c "import sys; print(sys.executable)"
```

确认包是否存在：

```powershell
uv run python -c "import importlib.util; print(importlib.util.find_spec('requests'))"
```

查看依赖：

```powershell
uv tree
```

常见原因：

- 在另一个 Python 环境中安装了包；
- VS Code 选择了错误解释器；
- 使用了 `pip install`，但运行时使用的是 uv 项目环境；
- 包名与 Python 导入名不同；
- 当前目录不是项目根目录。

### 17.6 依赖解析失败

先查看详细输出：

```powershell
uv sync -v
```

检查：

- `requires-python` 是否过严；
- 两个依赖是否要求冲突版本；
- 是否错误固定了某个旧版本；
- 是否需要预发布版本；
- 当前平台是否存在对应 wheel；
- 包是否需要本地 C/C++ 编译工具。

不要一开始就删除所有版本限制。应先根据错误信息确认具体冲突链。

### 17.7 下载慢或缓存异常

查看缓存目录：

```powershell
uv cache dir
```

清理某个包的缓存：

```powershell
uv cache clean package_name
```

清理全部缓存：

```powershell
uv cache clean
```

强制重新检查依赖：

```powershell
uv sync --refresh
```

注意：清理全部缓存后，后续可能需要重新下载依赖。

### 17.8 代理环境下无法下载

先查看代理变量：

```powershell
Get-ChildItem Env:HTTP_PROXY
Get-ChildItem Env:HTTPS_PROXY
```

当前 PowerShell 临时设置：

```powershell
$env:HTTP_PROXY = "http://127.0.0.1:XXXX"
$env:HTTPS_PROXY = "http://127.0.0.1:XXXX"
```

然后重试：

```powershell
uv sync
```

端口号应替换为本机代理软件实际监听的端口。

### 17.9 某些科学计算包安装失败

可能原因包括：

- 当前 Python 版本过新，没有对应 wheel；
- 包依赖 Visual C++ Build Tools；
- CUDA 版本或安装源不匹配；
- 包只支持特定平台；
- 需要系统级动态库。

解决思路：

1. 查看包官方安装说明；
2. 尝试使用更成熟的 Python 版本，例如 3.11 或 3.12；
3. 明确 CPU 版或 CUDA 版安装源；
4. 必要时继续使用 conda 管理底层二进制依赖；
5. 不要把所有安装失败都归因于 uv。

---

## 18. uv 与 conda 能否组合使用

可以，但要明确各自职责。

一种常见方式是：

```text
conda：提供 Python、CUDA、GDAL、FFmpeg 等底层环境
uv：安装和管理 Python 项目依赖
```

示例：

```powershell
conda create -n vision python=3.11
conda activate vision
uv venv --python "$env:CONDA_PREFIX\python.exe"
uv pip install -r requirements.txt
```

但这种组合会增加环境层次，初学阶段不要为了“工具更多”而强行叠加。

建议：

- 普通项目：优先只用 uv；
- 已经稳定运行的 conda 科研项目：不要急于迁移；
- 新建深度学习项目：先确认 CUDA/PyTorch 安装路线，再决定 uv 或 conda；
- 对环境可复现要求高时，必须记录 Python、CUDA、PyTorch 和驱动版本，而不能只保存 Python 依赖文件。

---

## 19. 推荐的新手工作流

### 19.1 创建新项目

```powershell
mkdir demo
cd demo
uv init
uv python pin 3.12
uv add requests
uv add --dev pytest ruff
uv run python main.py
```

### 19.2 日常添加依赖

```powershell
uv add package_name
```

### 19.3 日常运行代码

```powershell
uv run python main.py
```

### 19.4 日常运行测试

```powershell
uv run pytest
```

### 19.5 同事克隆项目后

```powershell
git clone <repository>
cd <repository>
uv sync
uv run python main.py
```

### 19.6 删除错误依赖

```powershell
uv remove package_name
```

### 19.7 查看环境

```powershell
uv tree
uv run python --version
uv run python -c "import sys; print(sys.executable)"
```

---

## 20. 面向已有项目的建议操作

你当前所在目录是：

```text
path\to\your\project
```

先查看项目已有文件：

```powershell
cd path\to\your\project
Get-ChildItem -Force
```

### 情况 A：已经存在 pyproject.toml 和 uv.lock

执行：

```powershell
uv sync
uv run python main.py
```

如果项目入口不是 `main.py`，根据 README 中的说明运行对应命令。

### 情况 B：只有 pyproject.toml，没有 uv.lock

执行：

```powershell
uv sync
```

uv 会解析依赖并创建 `uv.lock`。

### 情况 C：只有 requirements.txt

先采用保守方式：

```powershell
uv venv
uv pip install -r requirements.txt
```

如果确认项目可以迁移，再考虑：

```powershell
uv init
uv add -r requirements.txt
```

### 情况 D：目录中什么项目配置都没有

执行：

```powershell
uv init
uv add <项目需要的依赖>
uv run python main.py
```

> 不建议在没看项目文件的情况下直接执行 `uv init`。如果这是从 GitHub 克隆的项目，应先阅读 `README.md`，确认作者是否已经提供 `pyproject.toml`、`uv.lock` 或其他安装方式。

---

## 21. 常用命令速查表

| 目标 | 命令 |
|---|---|
| 查看 uv 版本 | `uv --version` |
| 更新 uv | `uv self update` |
| 初始化当前项目 | `uv init` |
| 创建新项目目录 | `uv init project_name` |
| 安装 Python 3.12 | `uv python install 3.12` |
| 固定项目 Python | `uv python pin 3.12` |
| 查看 Python 列表 | `uv python list` |
| 添加依赖 | `uv add requests` |
| 添加开发依赖 | `uv add --dev pytest` |
| 删除依赖 | `uv remove requests` |
| 生成或更新锁文件 | `uv lock` |
| 同步虚拟环境 | `uv sync` |
| 运行 Python 文件 | `uv run python main.py` |
| 查看依赖树 | `uv tree` |
| 创建传统虚拟环境 | `uv venv` |
| 从 requirements 安装 | `uv pip install -r requirements.txt` |
| 查看已安装包 | `uv pip list` |
| 临时运行工具 | `uvx ruff check .` |
| 导出 requirements | `uv export --format requirements.txt --output-file requirements.txt` |
| 查看缓存目录 | `uv cache dir` |
| 清理缓存 | `uv cache clean` |

---

## 22. 新手最容易犯的错误

### 错误一：在 uv 项目中仍然习惯性执行 pip install

不推荐：

```powershell
pip install requests
```

推荐：

```powershell
uv add requests
```

原因是 `uv add` 会同时更新项目声明和锁文件。

### 错误二：认为必须激活 .venv 才能使用 uv

不是。

可以直接：

```powershell
uv run python main.py
```

### 错误三：把 .venv 提交到 Git

不要提交 `.venv`。应提交：

```text
pyproject.toml
uv.lock
.python-version
```

### 错误四：只复制 .venv 到另一台电脑

虚拟环境通常不适合跨目录、跨电脑或跨操作系统复制。

正确方式：

```powershell
git clone ...
uv sync
```

### 错误五：同时随意维护 uv.lock 和 requirements.txt

推荐以 `uv.lock` 为主，需要时通过 `uv export` 生成 `requirements.txt`。

### 错误六：把 uv 当成完整 conda 替代品

uv 主要管理 Python 生态；复杂系统级依赖仍需操作系统包管理器、conda、Docker 或专门安装程序处理。

---

## 23. 一套最小可用操作模板

以后创建普通 Python 项目，可以直接参考下面这套命令：

```powershell
# 创建项目
mkdir my_project
cd my_project
uv init

# 固定 Python 版本
uv python pin 3.12

# 添加运行依赖
uv add requests pydantic

# 添加开发依赖
uv add --dev pytest ruff

# 运行程序
uv run python main.py

# 运行测试
uv run pytest

# 代码检查
uv run ruff check .

# 查看依赖
uv tree
```

另一台电脑复现环境：

```powershell
git clone <repository-url>
cd my_project
uv sync
uv run python main.py
```

---

## 24. 学习建议

不需要一次学习 uv 的全部功能。建议按下面顺序逐步掌握：

1. 安装并确认 `uv --version`；
2. 学会 `uv init`；
3. 学会 `uv add` 和 `uv remove`；
4. 学会 `uv run`；
5. 理解 `pyproject.toml`、`uv.lock` 和 `.venv`；
6. 学会 `uv sync`；
7. 再学习 Python 版本管理、依赖组、`uvx` 和导出功能。

对于刚从 conda/venv 迁移的用户，只要先记住下面四条命令，就可以开始使用：

```powershell
uv init
uv add package_name
uv sync
uv run python main.py
```

---

## 25. 官方参考资料

- uv 官方文档主页：<https://docs.astral.sh/uv/>
- 安装 uv：<https://docs.astral.sh/uv/getting-started/installation/>
- 项目管理入门：<https://docs.astral.sh/uv/guides/projects/>
- Python 版本管理：<https://docs.astral.sh/uv/guides/install-python/>
- 项目依赖管理：<https://docs.astral.sh/uv/concepts/projects/dependencies/>
- 锁定与同步：<https://docs.astral.sh/uv/concepts/projects/sync/>
- pip 兼容接口：<https://docs.astral.sh/uv/pip/>
- 从 pip 项目迁移：<https://docs.astral.sh/uv/guides/migration/pip-to-project/>
- Jupyter 集成：<https://docs.astral.sh/uv/guides/integration/jupyter/>
- 命令行参考：<https://docs.astral.sh/uv/reference/cli/>

---

## 结语

对于普通 Python 项目，uv 最核心的价值不是单纯“安装包更快”，而是把 Python 版本、项目依赖、虚拟环境、锁文件和运行命令整合成一套一致的工作流。

从传统习惯迁移时，不必立刻抛弃所有已有方法。可以先在一个小项目中使用：

```powershell
uv init
uv add
uv run
```

熟悉项目模式后，再逐步把旧的 `venv + pip + requirements.txt` 项目迁移为：

```text
pyproject.toml + uv.lock + .venv
```
